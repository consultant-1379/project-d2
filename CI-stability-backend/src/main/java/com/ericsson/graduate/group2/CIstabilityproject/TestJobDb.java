package com.ericsson.graduate.group2.CIstabilityproject;

/*
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.jdbc.core.JdbcTemplate;
import org.springframework.stereotype.Component;

import javax.annotation.PostConstruct;

@Component
public class TestJobDb {

    @Autowired
    JdbcTemplate jdbcTemplate;

    @PostConstruct
    public void init() {
        jdbcTemplate.execute(
                "create table JOBS(" +
                        "jobId int auto_increment primary key, " +
                        "jobName varChar)");

        jdbcTemplate.update("insert into JOBS (jobId, jobName) " +
                "values (?, ?)",
                new Object[]{1, "eric-oss-ran-topology-adapter_Publish"});
        jdbcTemplate.update("insert into JOBS (jobId, jobName) " +
                        "values (?, ?)",
                new Object[]{2, "eric-oss-ran-topology-adapter_PreCodeReview"});

    }


}

 */
